Design a method that takes a string as an input:
 
My name is Craig
 
and returns the a string with the order of the words in the sentence the same, but the order of the letters in the word reversed:
 
yM eman si giarC

//returns a string
function reverseString(string s){
  string newString = "";
  for(int i = s.length-1; i>=0; i--){
	newString = newString + s[i];
  }
  return newString;
}

function reverseWordsInSentence(string s){
  string[] tokens = s.split(" ");
  string result = "";
  for(int i = 0 i < token.length; i++) {
	result = result + reverseString(token[i]);
	if(i != token.length - 1){
	  result = result + " ";
	}
  }
  return result;
}
